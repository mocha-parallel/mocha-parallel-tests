'use strict';

describe('Test suite', () => {
    it('should end immediately', () => {});

    it('should fail', () => {
        throw new Error;
    });

    it('should end immediately', () => {});
    it('should end immediately', () => {});
});
