'use strict';

const assert = require('assert');

describe('tests file 2', () => {
    it('should be run 3. grep', () => {
        assert.ok(true);
    });

    it('should be skipped', () => {
        assert.ok(true);
    });
});

