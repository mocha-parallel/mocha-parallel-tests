'use strict';

describe('Check nested tests displays right - parent', function () {
    describe('Check nested tests displays right - child', function () {
        it('should be shild', function (done) {
            done();
        });
    });
});
