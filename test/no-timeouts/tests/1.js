'use strict';

describe('Run async test 2', function () {
    /* eslint-disable no-unused-vars */
    it('should fail with timeout', function (done) {
        setTimeout(done, 4000);
    });
    /* eslint-enable no-unused-vars */
});
